### 阶段 2：编辑 + Markdown 渲染 + TODO 基础 (Day 4-7) ⏳ 暂停中

**目标**：便签里能打字，支持 Markdown 渲染 + 勾选框 + 时间/优先级/分类标签

| 步骤 | 状态 | 内容 | 学习点 |
|------|------|------|--------|
| 2.1 | ✅ | 嵌入 Rich Edit 控件 `LoadLibrary("riched20.dll")` | 子窗口、控件创建 |
| 2.2~2.9 | ⏸️ | **暂缓**（先做存储位置 + 文件读写，再回来渲染 Markdown） | — |

> 执行顺序调整：先做阶段 3（存储位置）和阶段 4（文件读写），让便签能真正存盘。
> 再做 Markdown 渲染，那时有真实文件可以测试。

---

### 阶段 3：自定义存储位置 (Day 8) ⏳ 排期中

**目标**：用户可选择便签文件的存放目录，方便放入同步文件夹

| 步骤 | 状态 | 内容 | 学习点 |
|------|------|------|--------|
| 3.1 | | 确定便签文件存储路径 | `SHGetFolderPathW` 获取 `%APPDATA%` |
| 3.2 | | 默认值 `%APPDATA%\DeskNote\notes\` | 路径拼接、`CreateDirectoryW` 创建目录 |
| 3.3 | | 用户可改为自定义路径（如 `D:\OneDrive\笔记`） | `IFileOpenDialog` 文件夹选择对话框 |
| 3.4 | | 存储路径写入配置文件 | 简单 JSON 手写序列化 |
| 3.5 | | 下次启动时读取配置文件 | 简单 JSON 手写解析 |
| 3.6 | **里程碑**：便签文件有固定的存放位置，用户可选同步目录 |

**C/C++ 知识点**：
- `SHGetFolderPathW` 获取系统已知路径
- `CreateDirectoryW` 创建目录
- `IFileOpenDialog` COM 文件夹选择对话框
- `GetFileAttributesW` 路径有效性校验

---

### 阶段 4：文件读写 + 标题栏文件名 (Day 9-10) ⏳ 排期中

**目标**：便签内容能存盘，下次启动恢复

| 步骤 | 状态 | 内容 | 学习点 |
|------|------|------|--------|
| 4.1 | | 启动时遍历便签目录，列出所有 `.md` 文件 | `FindFirstFileW` / `FindNextFileW` |
| 4.2 | | 读取 `.md` 内容 → 显示到编辑器 | `ReadFile` / `SetWindowTextW` |
| 4.3 | | 定时自动保存（修改后 5 秒） | `SetTimer` + 脏标记 |
| 4.4 | | 标题栏显示当前文件名 | `DrawTextW` 改标题文字 |
| 4.5 | | **里程碑**：关掉重开，便签内容还在 |

**C/C++ 知识点**：
- `FindFirstFileW` / `FindNextFileW` 目录遍历
- `CreateFileW` / `ReadFile` / `WriteFile` 文件操作
- `GetModuleFileNameW` 获取 exe 所在路径
- 文件路径到文件名的提取

---

### 阶段 2（续）：Markdown 渲染 + TODO (Day 11-15) ⏳ 排期中

**目标**：便签内容能存能读后，再回来渲染 Markdown

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 2.3 | 编译引入 md4c 库（单文件 C，直接放项目里） | 第三方库集成、`.c` 和 `.c` 混编 |
| 2.4 | 实现 `parse_markdown()`：遍历 md4c 输出，转为 CHARFORMAT2 | 回调机制、状态机 |
| 2.5 | 编辑/预览双模式切换（双击标题栏） | 两个 Rich Edit 控件切换 |
| 2.6 | 渲染 **粗体** *斜体* `代码` # 标题 | CHARFORMAT2 的 `dwMask` / `dwEffects` |
| 2.7 | 解析 `- [ ]` `- [x]` → 渲染为 ☐ ☑，点击可切换 | `WM_LBUTTONUP` + 字符命中 |
| 2.8 | 解析 `@date` → 蓝色时间标签 | CHARFORMAT2 颜色设置 |
| 2.9 | 解析 `!priority` → 红/橙/灰优先级标签 | CHARFORMAT2 颜色设置 |
| 2.10 | 解析 `#tag` → 灰色分类标签（行内、`#` 后无空格） | CHARFORMAT2 颜色设置 |
| 2.11 | **里程碑**：打 Markdown 源码 + 标签，双击预览渲染效果 |

---

### 阶段 5：系统托盘 (Day 11-12) ⏳ 排期中

**目标**：托盘图标常驻，右键菜单操作

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 5.1 | 创建托盘图标 `Shell_NotifyIcon(NIM_ADD, ...)` | `NOTIFYICONDATA` 结构 |
| 5.2 | 托盘右键弹出菜单 `CreatePopupMenu` + `TrackPopupMenu` | HMENU 菜单句柄 |
| 5.3 | 菜单项：「新建便签」「显示全部」「隐藏全部」「退出」 | `WM_COMMAND` 子命令分发 |
| 5.4 | 任务栏隐藏 `WS_EX_TOOLWINDOW` | 扩展窗口样式 |
| 5.5 | 双击托盘图标显示/隐藏便签 | `NIN_SELECT` 通知 |
| 5.6 | **里程碑**：托盘常驻，右键管理 |

---

### 阶段 6：多便签管理 (Day 13-14)

**目标**：新建多个便签，彼此独立

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 6.1 | 抽象 `Note` 类（纯数据） | C++ 类的构造/析构、成员函数 |
| 6.2 | 抽象 `App` 类（全局状态） | `std::vector<Note>` 存储多个便签 |
| 6.3 | 新建便签：生成唯一 `.md` 文件名 | 时间戳命名 |
| 6.4 | 关闭便签：清理窗口 + 释放资源 | RAII 思想 |
| 6.5 | 遍历目录恢复所有便签 | `FindFirstFile` / `FindNextFile` |
| 6.6 | **里程碑**：同时打开 N 个独立便签 |

---

### 阶段 7：TODO 管理增强 (Day 15-17)

**目标**：便签不只是文本，还能做轻量任务管理

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 7.1 | 标题栏显示进度：`☑ 3/5 · 购物清单` | `WM_PAINT` 自定义绘制 |
| 7.2 | 快捷键 `Ctrl+Enter` 快速插入新任务行 | `WM_KEYDOWN`、`EM_REPLACESEL` |
| 7.3 | 右键菜单：「标记全部完成」「清除已完成」 | 遍历行、正则匹配 |
| 7.4 | 解析 `@date` 时间标签，过期标红 | `COLORREF`、GDI 画刷 |
| 7.5 | `@every` 周期任务完成后自动续期 | 日期计算、字符串拼接 |
| 7.6 | `!priority` 高优先级排到顶部 | 排序算法、文本重组 |
| 7.7 | 筛选模式：`Ctrl+F` 只看未完成任务 | Rich Edit 行高控制 |
| 7.8 | 导出纯任务列表到剪贴板 | `OpenClipboard` / `SetClipboardData` |
| 7.9 | **里程碑**：便签变身轻量 TODO 工具 |

---

### 阶段 8：今日待办汇总 (Day 18-19)

**目标**：跨便签聚合任务

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 8.1 | 托盘右键 → 今日待办：扫描 `@today` 和过期 | 多文件扫描、字符串搜索 |
| 8.2 | 弹出汇总窗口 | 列表控件 `CreateWindow(L"LISTBOX", ...)` |
| 8.3 | 双击任务 → 打开对应便签并滚动到该行 | `SetForegroundWindow` + `EM_SCROLL` |
| 8.4 | 托盘图标角标：过期任务数 | `Shell_NotifyIcon` balloon |
| 8.5 | 每日启动检查：过期通知 | 启动时扫描 + 通知 |
| 8.6 | **里程碑**：一站式查看所有待办 |

---

### 阶段 9：美化与交互优化 (Day 20-21)

**目标**：便签有颜色主题

| 步骤 | 内容 | 学习点 |
|------|------|--------|
| 9.1 | 颜色主题（黄/粉/蓝/绿/白），右键切换 | GDI 画刷 `CreateSolidBrush` |
| 9.2 | 窗口半透明 `SetLayeredWindowAttributes` | `WS_EX_LAYERED` |
| 9.3 | 置顶切换 `HWND_TOPMOST` | `SetWindowPos` Z-order |
| 9.4 | 自绘标题栏（颜色条 + 关闭按钮） | `WM_PAINT`、`BeginPaint` |
| 9.5 | 窗口阴影 `CS_DROPSHADOW` | 窗口类样式 |
| 9.6 | 右键菜单：置顶/不置顶、切换主题、删除便签 | `TrackPopupMenu` |
| 9.7 | **里程碑**：外观完整，像真正的桌面便签 |

---

### 阶段 10：跨平台抽象层 (Day 22-24)

**目标**：用宏封装平台差异

```
┌──────────────────────────────────┐
│         desknote 业务逻辑         │
├──────────────────────────────────┤
│         平台抽象层 (宏)           │
├──────────┬──────────┬────────────┤
│  win32   │  cocoa   │   gtk      │
└──────────┴──────────┴────────────┘
```

| 步骤 | 内容 |
|------|------|
| 10.1 | 设计 `platform.h` 宏接口 |
| 10.2 | 提取 Win32 实现到 `platform_win32.h` |
| 10.3 | 编写 macOS / Linux stub 实现 |
| 10.7 | **里程碑**：`-DPLATFORM=win32/macos/linux` 编译通过 |

---

### 阶段 11：Vim 模式 + 设置系统 (Day 25-27)

**目标**：Vim 风格纯键盘编辑 + 设置系统支持跨设备同步

---

## 学习路径总览

```
阶段 0  ─── 环境 + 编译流程
阶段 1  ─── 窗口 + 消息循环
阶段 2  ─── 编辑 + Markdown
阶段 3  ─── 自定义存储位置
阶段 4  ─── 文件 I/O + 自动保存
   ↓ 到这里已是一个完整可用的小工具
阶段 5  ─── 系统托盘
阶段 6  ─── 多便签 + UI/Core 分层
阶段 7  ─── TODO 管理增强
阶段 8  ─── 今日待办汇总
阶段 9  ─── GDI 自绘 + 视觉品质
阶段 10 ── 跨平台抽象层
阶段 11 ── Vim 模式 + 设置系统
```

---

## 项目结构

```
desknote/
├── CMakeLists.txt
├── lib/
│   └── widget.h / widget.c        # UI 构件骨架
├── src/
│   ├── main.c                     # WinMain + WndProc
│   └── UI/
│       ├── titlebar.h / .c        # 标题栏
│       ├── window.h / .c          # 窗口框架（resize、隐藏）
│       └── editor.h / .c          # Rich Edit 编辑器
├── resources/
│   └── desknote.rc                # 图标资源
└── docs/
    ├── DEVELOPMENT_PLAN.md        # 开发计划
    └── LEARNING_NOTES.md          # 学习笔记
```
